services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env.example
        required: true
      - path: .env
        required: false
    volumes:
      - cine_data:/app/data
    restart: unless-stopped

volumes:
  cine_data:
